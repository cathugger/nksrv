package tmplrenderer

/*
import (
	"nksrv/lib/app/webib0"
	"io"
	"strings"
	t "text/template"
)


type tagInfo struct {
	inhibit    bool // true: only end tag allowed; false: anything inside allowed
	override   bool // overrides normal line handling. doesn't mix at all with other tags, implies inhibit
	background bool // only affects background (mixes well with greentext)

	msgLineFmtCfg // only used when override is true
}

*/
