package nntp

/*
type Group interface{} // XXX

// num - number
// msgid - message-id
// id - either number or message-id or sometimes empty
type NNTPProvider interface {
	SetGroup(group string) (grp Group, num int64, lo int64, hi int64)
	ListGroup(w Responder, grp Group, group string, lo int64, hi int64)
	Last(w Responder, grp Group)
	Next(w Responder, grp Group)
	GetArticle(w Responder, grp Group, id string)
	GetHead(w Responder, grp Group, id string)
	GetBody(w Responder, grp Group, id string)
	Stat(w Responder, grp Group, id string)
	Post(w Responder, r io.Reader)                // XXX
	IHave(w Responder, msgid string, r io.Reader) // XXX
	NewGroups(w Responder, since time.Time)
	ListActive(w Responder, wildmat string)
	ListNewsgroups(w Responder, wildmat string)
	ListOverviewFmt(w Responder)
}
*/
